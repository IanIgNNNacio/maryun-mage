"""Maryun Replenishment — end-to-end pipeline.

This package is the single source of truth for all business logic.
CLI (``app.pipeline.run``) and Mage blocks (``mage/``) are thin wrappers
that call into these modules.
"""
__version__ = "0.1.0"
