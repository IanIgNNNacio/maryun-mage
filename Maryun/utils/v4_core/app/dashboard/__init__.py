"""Dashboard gerencial interactivo (Streamlit).

Capa de **solo lectura** sobre los outputs que ya generó el pipeline en
``data/output/<run_id>/``. No toca nada del flujo automático.

Ejecución local:
    streamlit run src/app/dashboard/app.py

Exposición pública: ver ``scripts/run_dashboard.bat`` + Cloudflare Tunnel.
"""
